{
  "name": "ios",
  "version": "1.0.0",
  "scripts": {
    "test": "echo 'no test required'",
    "rebuild-autoconsent": "rollup -c",
    "validate-defs-without-formatting": "validate-ddg-pixel-defs ./PixelDefinitions",
    "validate-pixel-defs": "validate-ddg-pixel-defs ./PixelDefinitions && prettier ./PixelDefinitions --check",
    "validate-pixel-debug-logs": "validate-ddg-pixel-logs",
    "validate-wide-event-debug-logs": "validate-ddg-wide-event-logs",
    "pixel-lint": "prettier ./PixelDefinitions --check",
    "pixel-lint.fix": "prettier ./PixelDefinitions --write"
  },
  "devDependencies": {
    "@duckduckgo/pixel-schema": "github:duckduckgo/pixel-schema#v2.1.0",
    "prettier": "^3.6.2"
  },
  "prettier": {
    "singleQuote": true,
    "printWidth": 140,
    "tabWidth": 4,
    "overrides": [
      {
        "files": "*.json5",
        "options": {
          "parser": "json",
          "tabWidth": 4
        }
      }
    ]
  }
}
